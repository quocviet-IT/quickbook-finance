// Stub for the `server-only` marker package, which is supplied by the Next.js
// bundler and does not resolve under vitest. Importing it must stay a no-op
// here so the real guard can remain in lib/ai/provider.ts.
export {};
